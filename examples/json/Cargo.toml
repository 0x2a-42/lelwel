[package]
name = "lelwel-json"
version = "0.0.0"
readme = "README.md"
license = "MIT OR Apache-2.0"
edition = "2024"

[lib]
crate-type = ["lib", "cdylib"]

[dependencies]
logos = "0.15"
codespan-reporting = "0.12"
wasm-bindgen = "0.2"

[build-dependencies]
lelwel = { path = "../.." }
