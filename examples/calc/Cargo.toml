[package]
name = "lelwel-calc"
version = "0.0.0"
readme = "README.md"
license = "MIT OR Apache-2.0"
edition = "2021"

[dependencies]
logos = "0.14.0"
codespan-reporting = "0.11.1"

[build-dependencies]
lelwel = { path = "../.." }
